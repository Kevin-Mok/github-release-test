    environment {
        GOPATH = "$WORKSPACE/go"
        PATH = "$PATH:$GOPATH/bin"
    }

                        echo $PATH
                        github-release info -u Kevin-Mok-Bot -r github-release-test
